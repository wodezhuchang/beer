# 方案A：数值特征单模态分类模型

## 一、方案概述

### 1.1 方案定位
本方案是啤酒风格分类任务的**基线方案**，仅使用啤酒的**结构化数值特征**进行分类。

### 1.2 核心思想
通过啤酒的物理属性（酒精度、苦味值、颜色等）来预测其风格类别，这是最直接、最快速的分类方式。

### 1.3 适用场景
- 需要快速上线的场景
- 计算资源有限的环境
- 文本数据质量较差时
- 作为后续复杂模型的性能基准

---

## 二、实现思路

### 2.1 特征选择

| 特征名称 | 数据类型 | 说明 |
|----------|----------|------|
| abv | Float | 酒精度（Alcohol By Volume） |
| ibu | Float | 苦味值（International Bitterness Units） |
| srmId | Integer | 颜色标准（Standard Reference Method） |
| glasswareId | Integer | 推荐饮用杯型 |
| availableId | Integer | 供应类型（全年/季节/限量） |

### 2.2 数据预处理流程

```
原始数据 → 缺失值处理 → 特征标准化 → 类别编码 → 数据集划分
```

**缺失值处理策略**：
- **数值特征**（abv, ibu）：使用中位数填充
- **类别特征**（srmId, glasswareId）：新增"未知"类别

**特征标准化**：
```python
# 使用StandardScaler标准化数值特征
# 转换后均值=0，标准差=1
scaler = StandardScaler()
scaled_features = scaler.fit_transform(numeric_features)
```

### 2.3 模型选择

| 模型名称 | 选择理由 |
|----------|----------|
| **XGBoost** | 梯度提升树，适合表格数据，效果稳定 |
| **LightGBM** | 内存效率高，训练速度快 |
| **Random Forest** | 集成学习，抗过拟合能力强 |

### 2.4 模型结构

```
输入层: [abv, ibu, srm, glassware, available]
    ↓
XGBoost决策树集成
    ↓
输出层: [style_1_prob, style_2_prob, ..., style_n_prob]
```

### 2.5 训练策略

| 参数 | 设置 |
|------|------|
| 学习率 | 0.1 |
| 树数量 | 100-500 |
| 最大深度 | 3-10 |
| 正则化 | L1/L2 |
| 早停 | patience=5 |

---

## 三、能解决的问题

### 3.1 快速分类问题
- **问题**：需要在短时间内对大量啤酒进行分类
- **解决方案**：XGBoost推理速度快，适合批量处理

### 3.2 数据质量问题
- **问题**：文本描述缺失或质量差
- **解决方案**：不依赖文本，仅使用结构化数据

### 3.3 可解释性需求
- **问题**：需要知道哪些特征影响分类结果
- **解决方案**：XGBoost提供特征重要性分析

### 3.4 资源受限场景
- **问题**：服务器内存/算力有限
- **解决方案**：模型体积小，训练和推理成本低

---

## 四、优缺点分析

### 4.1 优点

| 优点 | 说明 |
|------|------|
| **训练快速** | 数分钟内完成训练 |
| **部署简单** | 模型文件小，推理延迟低 |
| **可解释性强** | 特征重要性一目了然 |
| **鲁棒性好** | 对噪声数据容忍度高 |

### 4.2 缺点

| 缺点 | 影响 |
|------|------|
| **信息有限** | 仅使用5个特征，信息损失大 |
| **性能上限** | 难以突破85%准确率 |
| **无法理解文本** | 无法利用description中的语义信息 |

---

## 五、预期性能

| 指标 | 预期值 |
|------|--------|
| Accuracy | 75-80% |
| Macro-F1 | 70-75% |
| 推理时间 | <1ms/样本 |
| 模型大小 | <10MB |

---

## 六、适用业务场景

1. **啤酒推荐系统**：根据用户偏好的abv/ibu推荐相似啤酒
2. **库存管理**：快速分类新入库的啤酒
3. **数据清洗**：为缺失标签的数据自动标注
4. **实时API服务**：需要低延迟响应的场景
