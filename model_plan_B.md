# 方案B：文本特征单模态分类模型

## 一、方案概述

### 1.1 方案定位
本方案专注于**文本语义理解**，通过啤酒的描述文本（description）来预测风格类别。

### 1.2 核心思想
啤酒的描述文本包含丰富的风味、工艺、原料信息，这些是判断风格的关键依据。

### 1.3 适用场景
- 文本数据质量较高时
- 需要理解深层语义信息
- 数值特征缺失严重时

---

## 二、实现思路

### 2.1 特征来源

**description字段**包含的信息类型：
- 风味描述："柑橘香气"、"巧克力味"、"焦糖甜味"
- 酿造工艺："冷发酵"、"桶陈"、"干投酒花"
- 原料信息："大麦麦芽"、"西楚酒花"、"比利时酵母"
- 饮用建议："适合搭配海鲜"、"冷藏饮用"

### 2.2 文本预处理流程

```
原始文本 → 清洗 → 分词 → 向量化 → 模型输入
```

**文本清洗步骤**：
1. 去除HTML标签和特殊字符
2. 统一大小写
3. 去除停用词
4. 词形还原（Lemmatization）

### 2.3 模型选择

| 模型名称 | 适用阶段 |
|----------|----------|
| **BERT-base** | 标准语义理解 |
| **RoBERTa** | 更好的文本表示 |
| **DistilBERT** | 轻量级部署 |

### 2.4 模型结构

```
输入层: [CLS] token + 文本token
    ↓
BERT编码器 (12层Transformer)
    ↓
[CLS] token向量 (768维)
    ↓
分类头: 全连接层 + Softmax
    ↓
输出层: 风格类别概率分布
```

### 2.5 训练策略

| 参数 | 设置 |
|------|------|
| 预训练模型 | bert-base-uncased |
| 学习率 | 2e-5 |
| 批次大小 | 16-32 |
| 训练轮数 | 3-5 |
| 最大序列长度 | 128/256 |
| 优化器 | AdamW |

**微调策略**：
- 冻结底层Transformer层
- 仅训练顶层和分类头
- 后期解冻全部层进行微调

---

## 三、能解决的问题

### 3.1 语义理解问题
- **问题**：数值特征无法表达"柑橘香气"、"桶陈风味"等抽象概念
- **解决方案**：BERT能捕捉文本中的语义信息

### 3.2 细粒度分类问题
- **问题**：相似风格难以用数值特征区分
- **解决方案**：文本描述包含更细致的风格差异

### 3.3 数据缺失问题
- **问题**：部分啤酒缺少abv/ibu等数值特征
- **解决方案**：仅依赖description字段

### 3.4 迁移学习问题
- **问题**：样本数量不足时模型难以训练
- **解决方案**：利用BERT的预训练知识迁移

---

## 四、优缺点分析

### 4.1 优点

| 优点 | 说明 |
|------|------|
| **语义理解深** | 能捕捉文本中的细微差异 |
| **泛化能力强** | 迁移学习带来丰富的语言知识 |
| **特征自动提取** | 无需人工设计特征 |
| **性能上限高** | 适合复杂分类任务 |

### 4.2 缺点

| 缺点 | 影响 |
|------|------|
| **训练成本高** | 需要GPU资源，训练时间长 |
| **推理速度慢** | 单样本推理时间>50ms |
| **数据依赖** | 依赖高质量文本描述 |
| **可解释性差** | 黑盒模型，难以解释决策过程 |

---

## 五、预期性能

| 指标 | 预期值 |
|------|--------|
| Accuracy | 80-85% |
| Macro-F1 | 75-80% |
| 推理时间 | 50-100ms/样本 |
| 模型大小 | ~400MB |

---

## 六、适用业务场景

1. **智能客服**：理解用户描述的啤酒偏好
2. **啤酒评论分析**：从用户评论中提取风格特征
3. **新品分类**：根据产品描述自动分类新品
4. **市场调研**：分析市场上啤酒的风格趋势
