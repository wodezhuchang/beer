# 啤酒风格识别分类任务方案

## 1. 任务概述

### 1.1 任务目标
基于啤酒的属性特征（数值特征+文本描述），构建机器学习模型实现啤酒风格的自动识别与分类。

### 1.2 业务价值
- **自动标注**: 为大量未标注的啤酒数据自动分类
- **智能推荐**: 根据用户偏好推荐相似风格啤酒
- **市场分析**: 分析啤酒市场的风格分布趋势

### 1.3 任务范围
- **输入**: 啤酒的数值特征（abv, ibu等）+ 文本描述（description）
- **输出**: 啤酒风格类别（styleId）
- **任务类型**: 多分类任务

---

## 2. 数据准备方案

### 2.1 数据来源与规模
| 项目 | 说明 |
|------|------|
| 数据源 | `data/beer-dataset-master/beer-database/*.json` |
| 总记录数 | 30,121 条 |
| 风格类别数 | 约160+种风格 |
| 类别数 | 12个大类（category） |

### 2.2 数据划分策略

#### 2.2.1 划分比例
| 数据集 | 比例 | 数量（估算） | 用途 |
|--------|------|-------------|------|
| 训练集 | 70% | ~21,085 | 模型训练 |
| 验证集 | 15% | ~4,518 | 超参数调优 |
| 测试集 | 15% | ~4,518 | 最终性能评估 |

#### 2.2.2 划分原则
```
分层抽样（Stratified Sampling）
├── 按类别分布比例划分
├── 保持训练/验证/测试集的类别分布一致性
├── 避免数据泄露
```

#### 2.2.3 划分步骤
```python
# 步骤1: 加载所有数据并合并
# 步骤2: 按styleId分组统计
# 步骤3: 使用StratifiedShuffleSplit划分
# 步骤4: 保存划分后的数据集
# 步骤5: 验证类别分布一致性
```

### 2.3 特征工程方案

#### 2.3.1 特征类别

| 特征类型 | 特征名称 | 处理方式 |
|----------|----------|----------|
| **数值特征** | abv (酒精度) | 标准化/归一化 |
| | ibu (苦味值) | 标准化/归一化 |
| | srmId (颜色) | 类别编码/嵌入 |
| | glasswareId (杯型) | 类别编码/嵌入 |
| | availableId (供应类型) | 类别编码/嵌入 |
| **文本特征** | description (描述) | TF-IDF/BERT嵌入 |
| **类别特征** | styleId (目标) | 标签编码 |

#### 2.3.2 缺失值处理

| 字段 | 缺失率 | 处理策略 |
|------|--------|----------|
| abv | ~15% | 均值填充 / 中位数填充 |
| ibu | ~20% | 均值填充 / KNN填充 |
| srmId | ~30% | 众数填充 / 新建"未知"类别 |
| glasswareId | ~35% | 众数填充 / 新建"未知"类别 |
| description | ~5% | 空字符串 / 特殊标记 |

#### 2.3.3 特征选择策略
```
1. 统计特征相关性分析（Pearson/Spearman）
2. 使用XGBoost特征重要性筛选
3. 文本特征使用互信息筛选
4. 去除高度相关特征（VIF > 10）
```

---

## 3. 模型选择与架构

### 3.1 候选模型清单

| 模型类型 | 模型名称 | 适用场景 | 复杂度 |
|----------|----------|----------|--------|
| **传统ML** | Random Forest | 数值特征为主 | 低 |
| | XGBoost | 数值特征为主 | 中 |
| | LightGBM | 大规模数据 | 中 |
| **深度学习** | TextCNN | 文本分类 | 中 |
| | BERT | 文本理解 | 高 |
| | TabNet | 表格数据 | 中 |
| **多模态** | 融合模型 | 数值+文本 | 高 |

### 3.2 推荐模型架构

#### 方案A：单模态模型（数值特征）

```
输入层 (abv, ibu, srm, glassware, available)
    ↓
隐藏层1 (128 units, ReLU)
    ↓
隐藏层2 (64 units, ReLU)
    ↓
输出层 (num_classes units, Softmax)
```

#### 方案B：单模态模型（文本特征）

```
输入层 (description文本)
    ↓
BERT编码器 (预训练模型)
    ↓
[CLS] token向量
    ↓
分类层 (num_classes units, Softmax)
```

#### 方案C：多模态融合模型（推荐）

```
┌─────────────────────┐    ┌─────────────────────┐
│   数值特征分支      │    │    文本特征分支      │
│ abv, ibu, srm...   │    │    description      │
└─────────┬─────────┘    └─────────┬─────────┘
          │                        │
          ▼                        ▼
┌─────────────────────┐    ┌─────────────────────┐
│  全连接层 (128)     │    │    BERT编码器      │
└─────────┬─────────┘    └─────────┬─────────┘
          │                        │
          └───────────┬────────────┘
                      ▼
          ┌─────────────────────┐
          │    特征拼接层        │
          │   (Concatenation)   │
          └─────────┬─────────┘
                    ▼
          ┌─────────────────────┐
          │    分类头 (Softmax)  │
          └─────────────────────┘
```

---

## 4. 训练策略

### 4.1 训练参数设置

| 参数 | 值 | 说明 |
|------|------|------|
| 优化器 | AdamW | 权重衰减优化 |
| 学习率 | 1e-4 | 初始学习率 |
| 学习率调度 | CosineAnnealing | 余弦退火 |
| 批次大小 | 32/64 | 根据GPU显存调整 |
| 训练轮数 | 30-50 | 提前停止 |
| 损失函数 | CrossEntropyLoss | 多分类损失 |

### 4.2 正则化策略

| 方法 | 参数 | 作用 |
|------|------|------|
| Dropout | 0.3-0.5 | 防止过拟合 |
| L2正则 | 1e-4 | 权重衰减 |
| 早停 | patience=5 | 验证集性能下降时停止 |
| 数据增强 | 文本扰动 | 提升泛化能力 |

### 4.3 训练流程

```
数据加载 → 特征预处理 → 数据集划分 → 
模型初始化 → 训练循环 → 验证评估 → 
早停判断 → 模型保存
```

---

## 5. 性能评估方案

### 5.1 评估指标体系

#### 5.1.1 主指标（多分类）

| 指标 | 公式 | 含义 | 目标值 |
|------|------|------|--------|
| **Accuracy** | (TP+TN)/(TP+TN+FP+FN) | 整体准确率 | >85% |
| **Macro-F1** | 2*P*R/(P+R) 取平均 | 类别平均F1 | >80% |
| **Weighted-F1** | 加权平均F1 | 考虑类别不平衡 | >82% |
| **Top-K Accuracy** | 前K个预测包含正确类别 | Top-3 >95% |

#### 5.1.2 辅助指标

| 指标 | 用途 |
|------|------|
| **Confusion Matrix** | 分析类别混淆情况 |
| **Precision/Recall** | 针对特定类别分析 |
| **AUC-ROC** | 二分类场景下的排序能力 |
| **Log Loss** | 概率预测的校准程度 |

### 5.2 评估方法

#### 5.2.1 分层交叉验证

```python
# 使用StratifiedKFold进行5折交叉验证
# 每折保持类别分布一致
# 报告平均指标及标准差
```

#### 5.2.2 测试集评估流程

```
1. 使用最优模型在测试集上预测
2. 计算各项评估指标
3. 生成混淆矩阵可视化
4. 分析错误案例（混淆矩阵对角线外的样本）
5. 生成评估报告
```

### 5.3 评估报告模板

```
┌─────────────────────────────────────────────┐
│           啤酒风格分类模型评估报告           │
├─────────────────────────────────────────────┤
│ 模型名称: XGBoost / BERT / Fusion          │
│ 数据集: BeerDB (30,121 samples)            │
│ 类别数: 160+                                │
├─────────────────────────────────────────────┤
│ 评估指标                                    │
│ ├── Accuracy:     87.5%                    │
│ ├── Macro-F1:     82.3%                    │
│ ├── Weighted-F1:  84.7%                    │
│ └── Top-3 Acc:    96.2%                    │
├─────────────────────────────────────────────┤
│ 混淆矩阵分析                                │
│ ├── 易混淆类别对                            │
│ └── 最难分类类别                            │
├─────────────────────────────────────────────┤
│ 错误案例分析                                │
│ ├── 典型错误样本                            │
│ └── 改进建议                                │
└─────────────────────────────────────────────┘
```

---

## 6. 模型诊断与改进

### 6.1 诊断流程

```
1. 训练/验证曲线分析 → 判断过拟合/欠拟合
2. 混淆矩阵分析 → 识别易混淆类别
3. 特征重要性分析 → 发现关键特征
4. 错误案例分析 → 理解模型失败原因
5. 概率校准分析 → 评估预测置信度
```

### 6.2 常见问题与改进策略

| 问题 | 表现 | 改进策略 |
|------|------|----------|
| **过拟合** | 训练集Acc>95%，验证集<80% | 增加正则化、数据增强、减少模型复杂度 |
| **欠拟合** | 训练/验证集均<70% | 增加模型深度、添加特征、调整学习率 |
| **类别不平衡** | 少数类别Recall低 | SMOTE、加权损失、数据采集 |
| **文本质量差** | 短描述样本预测差 | 文本过滤、增强文本特征 |
| **特征噪声** | 无关特征影响预测 | 特征选择、正则化 |

### 6.3 迭代改进路径

```
V1.0: 基线模型（XGBoost + 数值特征）
    ↓ 目标: Accuracy >75%
    
V2.0: 添加文本特征（TF-IDF）
    ↓ 目标: Accuracy >80%
    
V3.0: 使用BERT文本嵌入
    ↓ 目标: Accuracy >83%
    
V4.0: 多模态融合模型
    ↓ 目标: Accuracy >85%
    
V5.0: 集成学习（Stacking）
    ↓ 目标: Accuracy >87%
```

---

## 7. 部署与监控

### 7.1 模型部署方案

| 组件 | 技术选型 | 说明 |
|------|----------|------|
| 模型格式 | ONNX / TensorRT | 优化推理速度 |
| API框架 | FastAPI | 轻量级API服务 |
| 容器化 | Docker | 环境一致性 |
| 负载均衡 | Nginx | 高并发支持 |

### 7.2 性能监控

| 监控指标 | 阈值 | 告警方式 |
|----------|------|----------|
| 推理延迟 | >100ms | 日志告警 |
| 服务可用性 | <99% | 邮件通知 |
| 预测准确率 | <80% | 自动触发重新训练 |

### 7.3 模型更新策略

```
定期更新流程：
1. 收集新数据
2. 评估模型性能衰减
3. 重新训练模型
4. A/B测试验证
5. 灰度发布
```

---

## 8. 预期成果与里程碑

### 8.1 阶段目标

| 阶段 | 时间 | 目标 | 交付物 |
|------|------|------|--------|
| 数据准备 | 1-2周 | 完成数据清洗和划分 | 数据集文件 |
| 基线模型 | 2-3周 | 建立基准性能 | 基线报告 |
| 模型优化 | 3-4周 | 达到85%准确率 | 优化模型 |
| 融合模型 | 2-3周 | 多模态融合 | 最终模型 |
| 部署上线 | 1-2周 | API服务部署 | 部署文档 |

### 8.2 成功标准

```
✅ 测试集Accuracy ≥ 85%
✅ Macro-F1 ≥ 82%
✅ Top-3 Accuracy ≥ 95%
✅ 推理延迟 < 100ms
✅ 模型可解释性分析报告
```

---

## 9. 风险与应对

| 风险 | 概率 | 影响 | 应对策略 |
|------|------|------|----------|
| 数据质量差 | 高 | 模型性能受限 | 严格数据清洗流程 |
| 类别不平衡 | 中 | 少数类别预测差 | SMOTE、加权损失 |
| 模型过拟合 | 中 | 泛化能力差 | 正则化、早停 |
| 计算资源不足 | 低 | 训练周期长 | 分布式训练、模型压缩 |
| 业务需求变更 | 低 | 方案调整 | 模块化设计 |

---

## 10. 附录

### 10.1 风格类别分布示例

| 类别名称 | 样本数 | 占比 |
|----------|--------|------|
| American IPA | ~3,000 | 10% |
| American Pale Ale | ~2,500 | 8% |
| American Stout | ~2,000 | 7% |
| Belgian Tripel | ~1,500 | 5% |
| ... | ... | ... |

### 10.2 关键特征重要性预期

```
数值特征重要性排序（预期）:
1. abv (酒精度)
2. ibu (苦味值)
3. srm (颜色)
4. glassware (杯型)
5. available (供应类型)

文本特征主题（预期）:
- 风味描述（果香、麦芽、苦味）
- 酿造工艺（发酵方式、原料）
- 产地信息（地域特征）
```
